apiVersion: v1
kind: ConfigMap

metadata:
  name: postgres-init
  namespace: ecommerce

data:
  init.sql: |
    CREATE DATABASE customer;
    CREATE DATABASE product;
    CREATE DATABASE orders;
    CREATE DATABASE notification;
